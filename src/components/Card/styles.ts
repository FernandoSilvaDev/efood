import styled from 'styled-components'
import { colors } from '../../styles'

export const Container = styled.div`
  h2 {
    color: ${colors.beige};
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 16px;
  }

  p {
    font-size: 14px;
    font-weight: 400;
    color: ${colors.beige};
    line-height: 22px;
    margin: 16px 0;
  }
`
